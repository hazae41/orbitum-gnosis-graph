type Profile @entity {
  id: ID!
  address: String!
  nickname: String
  avatar: String
  description: String
}

type Forum @entity {
  id: ID!
  name: String!
}

type Topic @entity {
  id: ID!
  forum: ID!
  author: Profile!
  title: String!
}

type Post @entity {
  id: ID!
  topic: Topic!
  author: Profile!
  text: String!
}